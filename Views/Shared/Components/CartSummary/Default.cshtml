@model int

<a class="nav-link text-dark" asp-controller="Cart" asp-action="Index">
    <i class="fas fa-shopping-cart"></i> Cart
    @if (Model > 0)
    {
        <span class="badge bg-primary rounded-pill">@Model</span>
    }
</a>
